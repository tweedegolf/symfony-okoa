#!/usr/bin/env bash
cd docs

case "$1" in
    "server")
        sphinx-autobuild --poll -H 0.0.0.0 -p 2080 . _build
        ;;
    "build")
        make html
        ;;
    *)
        echo "Usage: $0 [server|build]"
        exit 1
        ;;
esac

