default:
    paths:
        features: features
    filters:
        tags: "~@tbd"
    extensions:
        Behat\Symfony2Extension\Extension:
            mink_driver: true
        Behat\MinkExtension\Extension:
            default_session: symfony2
wip:
    filters:
        tags: "@wip"
